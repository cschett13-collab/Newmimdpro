#!/usr/bin/env bash
# Opens your Zenvy bot's Telegram chat in the Telegram app (or browser fallback).
# EDIT THIS to match your bot username from @BotFather (without the leading @):
BOT_USERNAME="YourBotUsername"

open "https://t.me/${BOT_USERNAME}"
